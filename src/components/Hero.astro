---
import { Button } from "@/components/ui/button";
import Newsletter from './Newsletter.astro';
import CodeEditor from './CodeEditor.astro';
import '@/styles/globals.css';
---

<section class="pt-32 pb-20 hero-gradient">
  <div class="container mx-auto px-4">
    <div class="flex flex-col md:flex-row items-center justify-between gap-12">
      <div class="md:w-1/2 space-y-6">
        <h1 class="text-4xl md:text-5xl lg:text-6xl font-bold leading-tight">
          Technology that speaks <span class="text-gradient">your language</span>
        </h1>
        <p class="text-lg md:text-xl text-muted-foreground">
          We're bridging language gaps with AI solutions that understand and respond in your native tongue, rewriting who gets to write the future.
        </p>
        <div class="flex flex-col sm:flex-row gap-4">
          <Button variant="default" size="lg" className="secondary-button">Get Started Free</Button>
          <Button variant="default" size="lg" className="primary-button">See Docs</Button>
        </div>
        <div class="pt-6">
          <p class="text-sm text-muted-foreground">Code in your language, collaborate across 7,000+ languages</p>
          <div class="flex flex-wrap gap-8 items-center mt-4">
            {['عربي', 'తెలుగు', 'Español', '普通话'].map((language) => (
              <div class="text-muted-foreground/70 font-medium">{language}</div>
            ))}
          </div>
        </div>
      </div>
      <div class="md:w-1/2">
        <div class="relative">
          <div class="absolute -top-10 -left-10 w-40 h-40 bg-brand-coral/10 rounded-full blur-3xl"></div>
          <div class="absolute -bottom-10 -right-10 w-40 h-40 bg-brand-cyan/10 rounded-full blur-3xl"></div>
          <CodeEditor />
        </div>
      </div>
    </div>
  </div>
</section>

<div class="relative">
  <!-- Background decoration -->
  <!-- <div class="absolute inset-0 -z-10 mx-0 max-w-none overflow-hidden">
    <div class="absolute inset-0 bg-brand-cyan opacity-40 [mask-image:radial-gradient(farthest-side_at_top,white,transparent)] dark:bg-brand-cyan/30 dark:opacity-100">
      <svg aria-hidden="true" class="absolute inset-x-0 inset-y-[-50%] h-[200%] w-full skew-y-[-18deg] fill-black/40 stroke-black/50 mix-blend-overlay dark:fill-white/2.5 dark:stroke-white/5">
        <defs>
          <pattern id="e813992c-7d03-4cc4-a2bd-151760b470a0" width="72" height="56" patternUnits="userSpaceOnUse" x="50%" y="100%">
            <path d="M.5 56V.5H72" fill="none" />
          </pattern>
        </defs>
        <rect width="100%" height="100%" stroke-width="0" fill="url(#e813992c-7d03-4cc4-a2bd-151760b470a0)" />
      </svg>
    </div>
  </div> -->

  <!-- Content -->
  <div class="container relative pt-20 sm:pt-24 lg:pt-32">
    <div class="mx-auto max-w-2xl lg:max-w-4xl lg:px-12">
      <h1 class="font-display text-5xl font-bold tracking-tighter text-primary sm:text-7xl">
        <span class="block text-center">Code in Your</span>
        <span class="block text-center bg-gradient-to-r from-brand-coral to-brand-background-coral bg-clip-text text-transparent pb-1">Language</span>
      </h1>
      <p class="mt-6 text-center text-lg text-muted-foreground">
        Legesher bridges the gap between human languages and programming languages. 
        Write code in your native language, collaborate globally, and make programming truly universal.
      </p>
      <div class="mt-8 flex justify-center gap-4">
        <Button variant="default" size="lg" className="primary-button">
          Get Started
        </Button>
        <Button variant="outline" size="lg" className="outline-button">
          View on GitHub
        </Button>
      </div>
    </div>
  </div>
</div> 